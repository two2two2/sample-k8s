apiVersion: v1
kind: ConfigMap
metadata:
  namespace: default
  name: config
data:
  RAILS_ENV: "development"
  # DB_HOST: "dbserver-service.default.svc.cluster.local"
  DB_HOST: "dbserver-service"
  MYSQL_USER: "guest_user"
  MYSQL_PASSWORD: "password"
  MYSQL_ROOT_PASSWORD: "password"
